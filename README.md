# Venue_Event_Organiser
Software Engineering project.

## TODO

- Complete the project-wide JavaDoc cleanup: add missing `@param`, `@return`, and `@throws` tags, wrap generic types with `{@code ...}`, and fix declarations that do not match the documented exceptions.
